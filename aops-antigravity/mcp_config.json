{
  "mcpServers": {
    "task_manager": {
      "command": "env",
      "args": [
        "-u",
        "VIRTUAL_ENV",
        "uv",
        "run",
        "--directory",
        "${extensionPath}",
        "python",
        "mcp_servers${/}tasks_server.py"
      ],
      "env": {
        "ACA_DATA": "${ACA_DATA}"
      }
    }
  }
}